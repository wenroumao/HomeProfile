# 网易云音乐组件功能实现文档

## 📋 项目概述

本文档详细介绍如何在 Next.js 项目中实现网易云音乐听歌记录展示功能，包括：
- 显示用户最近一周的听歌记录
- 支持显示更多歌曲（默认50首，可配置到100首）
- 播放次数统计
- 响应式设计和滚动浏览
- 多语言支持

## 🛠️ 技术栈

- **前端框架**: Next.js 14+ (App Router)
- **UI组件**: shadcn/ui
- **样式**: Tailwind CSS
- **国际化**: react-i18next
- **API**: 网易云音乐第三方API
- **TypeScript**: 类型安全

## 📁 项目结构

```
HomeProfile/
├── app/
│   └── api/
│       └── netease-music/
│           └── route.ts          # 网易云音乐API路由
├── components/
│   └── netease-music-stats.tsx   # 网易云音乐组件
├── i18n/
│   └── locales/
│       ├── zh.json               # 中文翻译
│       └── en.json               # 英文翻译
└── .env.local                    # 环境变量配置
```

## 🚀 实现步骤

### 步骤1: 环境配置

#### 1.1 创建环境变量文件

在项目根目录创建 `.env.local` 文件：

```env
# 网易云音乐配置
NETEASE_USER_ID=你的网易云用户ID
NETEASE_MUSIC_U=你的MUSIC_U_Cookie值
```

#### 1.2 获取网易云用户ID

1. 打开网易云音乐网页版：https://music.163.com/
2. 登录你的账号
3. 进入个人主页，URL中的数字就是你的用户ID
   - 例如：`https://music.163.com/user/home?id=1881146945`
   - 用户ID就是：`1881146945`

#### 1.3 获取MUSIC_U Cookie

1. 在网易云音乐网页版登录状态下
2. 按F12打开开发者工具
3. 切换到 `Application` 标签页
4. 左侧选择 `Cookies` → `https://music.163.com`
5. 找到名为 `MUSIC_U` 的Cookie，复制其值

### 步骤2: 创建API路由

创建文件 `app/api/netease-music/route.ts`：

```typescript
import { NextResponse } from 'next/server';

// 内存缓存对象
const neteaseCache: Record<string, { data: any, timestamp: number }> = {};
const CACHE_DURATION = 12 * 60 * 60 * 1000; // 12小时

// 检查是否为强制刷新请求
function isHardReload(request: Request): boolean {
  const cacheControl = request.headers.get('Cache-Control');
  return Boolean(cacheControl?.includes('no-cache') || cacheControl?.includes('max-age=0'));
}

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  // 支持从query或环境变量获取uid
  const uid = searchParams.get('uid') || process.env.NETEASE_USER_ID;
  const baseURL = 'https://neteasecloudmusicapi.wenroumao.com';
  
  // 检查是否为强制刷新
  const isForceRefresh = isHardReload(request);
  
  if (!uid) {
    console.error('未提供网易云用户ID');
    return NextResponse.json({ code: 400, message: '缺少uid参数' }, { status: 400 });
  }

  // 使用缓存，除非是强制刷新
  const cacheKey = `netease-${uid}`;
  const now = Date.now();
  const cached = neteaseCache[cacheKey];
  if (cached && (now - cached.timestamp < CACHE_DURATION) && !isForceRefresh) {
    console.log('使用网易云音乐API缓存数据');
    return NextResponse.json({ code: 200, data: cached.data });
  }

  try {
    const musicU = process.env.NETEASE_MUSIC_U;
    
    if (!musicU || musicU === 'your_actual_music_u_cookie_value_here') {
      console.error('未配置有效的MUSIC_U Cookie');
      return NextResponse.json({ 
        code: 400, 
        message: '未配置有效的MUSIC_U Cookie。请在.env.local文件中设置正确的NETEASE_MUSIC_U值。' 
      }, { status: 400 });
    }

    // 使用获取到的MUSIC_U请求网易云API
    const apiUrl = `${baseURL}/user/record?uid=${uid}&type=1`;
    
    const res = await fetch(apiUrl, {
      headers: {
        'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/123.0.0.0 Safari/537.36',
        'Referer': 'https://music.163.com/',
        'Cookie': `MUSIC_U=${musicU}`
      }
    });
    
    const data = await res.json();
    console.log('网易云API响应:', JSON.stringify(data, null, 2));

    if (data.code !== 200) {
      console.error('网易云API返回错误:', data);
      return NextResponse.json({ 
        code: data.code, 
        message: data.message || '网易云API错误。可能原因：1) MUSIC_U Cookie无效或过期 2) 用户ID不正确 3) API服务不可用'
      }, { status: 500 });
    }

    const weekData = data.weekData || [];
    // 增加显示的歌曲数量到100首，并支持通过查询参数自定义
    const limit = parseInt(searchParams.get('limit') || '100');
    const songList = weekData.slice(0, limit).map((record: any, idx: number) => {
      const song = record.song;
      return {
        rank: idx + 1,
        id: song.id,
        name: song.name,
        artists: song.ar.map((a: any) => a.name),
        album: song.al.name,
        playCount: record.playCount,
        score: record.score,
        duration: song.dt,
        cover: song.al.picUrl
      };
    });

    // 写入缓存
    neteaseCache[cacheKey] = { data: songList, timestamp: now };
    return NextResponse.json({ code: 200, data: songList });
  } catch (e: any) {
    console.error('网易云API请求失败:', e);
    return NextResponse.json({ code: 500, message: e.message || '服务器内部错误' }, { status: 500 });
  }
}
```

### 步骤3: 创建前端组件

创建文件 `components/netease-music-stats.tsx`：

```typescript
"use client"

import { useState, useEffect, memo } from "react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { useTranslation } from "react-i18next"
import { SiNeteasecloudmusic } from "react-icons/si"

interface SongRecord {
  id: number;
  name: string;
  artists: string[];
  album: string;
  playCount: number;
  score: number;
  duration: number;
  cover: string;
}

// 缓存键
const NETEASE_CACHE_KEY = 'netease_music_data';
// 缓存过期时间（30分钟）
const CACHE_EXPIRY = 30 * 60 * 1000;

// 使用memo优化组件，避免不必要的重新渲染
export const NeteaseMusicStats = memo(function NeteaseMusicStats() {
  const [records, setRecords] = useState<SongRecord[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)
  const { t } = useTranslation()

  useEffect(() => {
    const fetchData = async () => {
      try {
        setLoading(true)
        setError(null)
        
        const now = Date.now();
        
        // 检查本地缓存
        const cached = localStorage.getItem(NETEASE_CACHE_KEY);
        if (cached) {
          const { records: cachedRecords, timestamp } = JSON.parse(cached);
          if (now - timestamp < CACHE_EXPIRY) {
            console.log('使用本地缓存的网易云音乐数据');
            setRecords(cachedRecords);
            setLoading(false);
            return;
          }
        }

        // 获取用户ID
        const profileRes = await fetch('/api/profile-public');
        const profileData = await profileRes.json();
        const uid = profileData.netease_user_id;
        
        if (!uid) {
          setError(t('neteaseMusic.noUserId'));
          setLoading(false);
          return;
        }
        
        // 获取最近一周听歌记录
        const res = await fetch(`/api/netease-music?uid=${uid}`);
        const data = await res.json();
        if (!data.data || data.data.length === 0) {
          setError(t('neteaseMusic.noRecords'));
          setLoading(false);
          return;
        }
        
        setRecords(data.data)
        
        // 保存到本地缓存
        localStorage.setItem(NETEASE_CACHE_KEY, JSON.stringify({
          records: data.data,
          timestamp: now
        }));
        console.log('网易云音乐数据已缓存，有效期30分钟');
      } catch (e: any) {
        console.error('获取网易云音乐数据失败:', e);
        setError(t('neteaseMusic.fetchError') + (e.message || '未知错误'));
      } finally {
        setLoading(false)
      }
    }
    fetchData()
  }, [t])

  if (loading) {
    return (
      <Card className="bg-white/[.60] dark:bg-black/[.30] border border-white/10 shadow-xl rounded-2xl flex flex-col justify-center items-center p-4 transition-all hover:shadow-2xl hover:scale-[1.01] w-full h-full">
        <CardHeader className="pt-0 pb-2">
          <CardTitle className="flex items-center gap-2 text-lg">
            <SiNeteasecloudmusic className="text-red-500 h-5 w-5" /> {t("neteaseMusic.title")}
          </CardTitle>
        </CardHeader>
        <CardContent className="flex-1 flex flex-col justify-center items-center w-full px-0 pb-0">
          <div className="space-y-3 w-full max-w-xs">
            {[...Array(3)].map((_, i) => (
              <div key={i} className="flex items-center gap-3 animate-pulse">
                <div className="h-10 w-10 bg-muted rounded-full" />
                <div className="flex-1 space-y-1">
                  <div className="h-4 bg-muted rounded w-3/4" />
                  <div className="h-3 bg-muted rounded w-1/2" />
                </div>
              </div>
            ))}
          </div>
        </CardContent>
      </Card>
    )
  }

  if (error) {
    return (
      <Card className="bg-white/[.60] dark:bg-black/[.30] border border-white/10 shadow-xl rounded-2xl flex flex-col justify-center items-center p-4 transition-all hover:shadow-2xl hover:scale-[1.01] w-full h-full">
        <CardHeader className="pb-2 pt-0">
          <CardTitle className="flex items-center gap-2 text-lg">
            <SiNeteasecloudmusic className="text-red-500 h-5 w-5" /> {t("neteaseMusic.title")}
          </CardTitle>
        </CardHeader>
        <CardContent className="flex-1 flex flex-col justify-center items-center text-center">
          <p className="text-sm text-destructive">{error}</p>
        </CardContent>
      </Card>
    )
  }

  if (records.length === 0) {
    return (
      <Card className="bg-white/[.60] dark:bg-black/[.30] border border-white/10 shadow-xl rounded-2xl flex flex-col justify-center items-center p-4 transition-all hover:shadow-2xl hover:scale-[1.01] w-full h-full">
        <CardHeader className="pb-2 pt-0">
          <CardTitle className="flex items-center gap-2 text-lg">
            <SiNeteasecloudmusic className="text-red-500 h-5 w-5" /> {t("neteaseMusic.title")}
          </CardTitle>
        </CardHeader>
        <CardContent className="flex-1 flex flex-col justify-center items-center text-center">
          <p className="text-sm text-muted-foreground">{t("neteaseMusic.noData")}</p>
        </CardContent>
      </Card>
    )
  }

  // 展示最近一周听歌明细，支持滚动浏览
  return (
    <Card className="bg-white/[.60] dark:bg-black/[.30] border border-white/10 shadow-xl rounded-2xl flex flex-col p-2 sm:p-4 transition-all hover:shadow-2xl hover:scale-[1.01] w-full h-full overflow-hidden">
      <CardHeader className="pt-2 pb-3 px-2 sm:px-4">
        <CardTitle className="flex items-center gap-2 text-lg sm:text-xl">
          <SiNeteasecloudmusic className="text-red-500 h-5 w-5 sm:h-6 sm:w-6" /> {t("neteaseMusic.title")}
        </CardTitle>
      </CardHeader>
      <CardContent className="bg-transparent pt-0 px-2 sm:px-4 pb-2 sm:pb-4 w-full flex-1 overflow-hidden">
        <h4 className="text-sm sm:text-md font-semibold mb-2 sm:mb-3 flex items-center gap-2">
          <span className="inline-block w-2 h-2 rounded-full bg-red-500" />
          {t('neteaseMusic.recentPlaysTitle')} ({records.length} {t('neteaseMusic.songsUnit')})
        </h4>
        <div className="h-full overflow-y-auto pr-2">
          <ul className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6 gap-2 sm:gap-3">
            {records.map((song) => (
              <li key={song.id}>
                <a
                  href={`https://music.163.com/song?id=${song.id}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex items-center gap-2 sm:gap-3 group hover:bg-white/20 dark:hover:bg-black/30 rounded-lg p-1.5 sm:p-2 transition"
                >
                  <img
                    src={song.cover}
                    alt={song.name}
                    className="h-10 w-10 sm:h-12 sm:w-12 rounded shadow object-cover flex-shrink-0"
                  />
                  <div className="min-w-0 flex-1">
                    <p className="font-medium text-xs sm:text-sm text-foreground truncate">{song.name}</p>
                    <p 
                      className="text-xs text-muted-foreground truncate" 
                      style={{ textShadow: '0px 0px 5px rgba(0,0,0,0.7)' }}
                    >
                      {song.artists.join(' / ')} · {song.playCount}{t('neteaseMusic.playCountUnit')}
                    </p>
                  </div>
                </a>
              </li>
            ))}
          </ul>
        </div>
      </CardContent>
    </Card>
  )
})
```

### 步骤4: 配置国际化翻译

#### 4.1 中文翻译 (`i18n/locales/zh.json`)

在 `neteaseMusic` 部分添加：

```json
{
  "neteaseMusic": {
    "title": "网易云音乐排行",
    "unburdenHeart": "解开超载的心",
    "noUserId": "未配置网易云用户ID，请在settings.json中设置netease_user_id字段",
    "noRecords": "未获取到听歌记录",
    "fetchError": "获取数据失败:",
    "recentPlaysTitle": "最近一周听歌",
    "playCountUnit": "次",
    "songsUnit": "首"
  }
}
```

#### 4.2 英文翻译 (`i18n/locales/en.json`)

在 `neteaseMusic` 部分添加：

```json
{
  "neteaseMusic": {
    "title": "NetEase Music Ranking",
    "unburdenHeart": "Unburden the Overloaded Heart",
    "noUserId": "NetEase user ID not configured, please set netease_user_id in settings.json",
    "noRecords": "No listening records obtained",
    "fetchError": "Failed to fetch data:",
    "recentPlaysTitle": "Recent Plays (Last Week)",
    "playCountUnit": "plays",
    "songsUnit": "songs"
  }
}
```

### 步骤5: 在页面中使用组件

在你的页面文件中导入并使用组件：

```typescript
import { NeteaseMusicStats } from '@/components/netease-music-stats'

export default function HomePage() {
  return (
    <div className="container mx-auto p-4">
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <div className="h-96">
          <NeteaseMusicStats />
        </div>
        {/* 其他组件 */}
      </div>
    </div>
  )
}
```

## 🎯 功能特性

### 1. 可配置显示数量
- 默认显示100首歌曲
- 支持通过URL参数自定义：`/api/netease-music?uid=xxx&limit=50`

### 2. 智能缓存机制
- **服务端缓存**: 12小时，减少API调用
- **客户端缓存**: 30分钟，提升用户体验
- **强制刷新**: 支持Ctrl+F5强制刷新

### 3. 响应式设计
- 移动端友好的网格布局
- 自适应不同屏幕尺寸
- 流畅的滚动体验

### 4. 错误处理
- 详细的错误提示信息
- 优雅的加载状态
- 网络异常处理

## 🔧 自定义配置

### 修改缓存时间

在 `components/netease-music-stats.tsx` 中：

```typescript
// 修改客户端缓存时间（毫秒）
const CACHE_EXPIRY = 30 * 60 * 1000; // 30分钟

// 在 route.ts 中修改服务端缓存时间
const CACHE_DURATION = 12 * 60 * 60 * 1000; // 12小时
```

### 修改默认显示数量

在 `app/api/netease-music/route.ts` 中：

```typescript
// 修改默认显示数量
const limit = parseInt(searchParams.get('limit') || '50'); // 改为50首
```

### 自定义样式

组件使用 Tailwind CSS，可以通过修改 className 来自定义样式：

```typescript
// 修改卡片样式
<Card className="your-custom-classes">

// 修改网格布局
<ul className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-4">
```

## 🐛 常见问题

### 1. 显示"未配置有效的MUSIC_U Cookie"

**解决方案**:
- 确保 `.env.local` 文件中的 `NETEASE_MUSIC_U` 值正确
- 重新获取 MUSIC_U Cookie（Cookie可能过期）
- 重启开发服务器

### 2. 显示"未获取到听歌记录"

**解决方案**:
- 检查网易云用户ID是否正确
- 确保该用户有听歌记录
- 检查网络连接

### 3. 组件不显示或显示错误

**解决方案**:
- 检查控制台错误信息
- 清除浏览器缓存：`localStorage.removeItem('netease_music_data')`
- 检查翻译文件是否正确配置

### 4. API请求失败

**解决方案**:
- 检查第三方API服务是否可用
- 验证Cookie是否有效
- 检查网络防火墙设置

## 📱 移动端优化

组件已针对移动端进行优化：

- 响应式网格布局
- 触摸友好的交互
- 适配小屏幕的字体大小
- 流畅的滚动体验

## 🚀 部署注意事项

### Vercel部署

1. 确保环境变量在Vercel控制台中正确配置
2. 检查API路由是否正常工作
3. 验证第三方API的访问权限

### 其他平台部署

1. 配置相应的环境变量
2. 确保Node.js版本兼容
3. 检查网络访问策略

## 📈 性能优化建议

1. **使用CDN**: 为音乐封面图片配置CDN
2. **图片优化**: 使用Next.js Image组件优化图片加载
3. **懒加载**: 对长列表实现虚拟滚动
4. **缓存策略**: 根据使用场景调整缓存时间

## 🔒 安全考虑

1. **Cookie安全**: 不要在客户端暴露MUSIC_U Cookie
2. **API限制**: 实现适当的API调用频率限制
3. **错误处理**: 避免在错误信息中暴露敏感信息
4. **CORS配置**: 正确配置跨域访问策略

## 📝 更新日志

- **v1.0.0**: 基础功能实现
- **v1.1.0**: 增加显示歌曲数量到100首
- **v1.2.0**: 优化缓存机制，从12小时改为30分钟
- **v1.3.0**: 添加滚动浏览和歌曲计数显示

---

## 💡 小贴士

1. **开发调试**: 使用浏览器开发者工具查看网络请求和控制台日志
2. **缓存清理**: 开发时可以通过控制台执行 `localStorage.clear()` 清除所有缓存
3. **API测试**: 可以直接访问 `http://localhost:3000/api/netease-music?uid=你的用户ID` 测试API
4. **样式调试**: 使用浏览器开发者工具实时调整CSS样式

通过以上步骤，你就可以成功实现一个功能完整的网易云音乐听歌记录展示组件了！🎵